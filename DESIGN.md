# here — 기획서 · 설계서

긴급 상황과 그 직전을 위한 위치 기록·공유 앱.

이 문서는 **왜 이렇게 만들었는가**를 남긴다. 기능 목록은 `README.md`,
설치 절차는 `SETUP.md`, 빌드 준비는 `EAS.md` 에 있다. 여기서는 그 문서들이
설명하지 않는 판단의 근거와 구조를 다룬다.

| 문서 | 답하는 질문 |
|---|---|
| `README.md` | 뭘 할 수 있나 |
| `SETUP.md` | 어떻게 깔고 연결하나 |
| `EAS.md` | 폰에 어떻게 올리나 |
| **`DESIGN.md`** | **왜 이렇게 만들었나** |

작성 시점 기준 규모: 앱·웹·스키마 합쳐 약 8,800줄, Supabase 테이블 7개,
RPC 함수 20개, 앱 화면 12개.

---

# 1부. 기획

## 1.1 문제

이 앱은 세 가지 상황을 겨냥한다.

1. **술 취해 잃어버린 기억을 더듬는 밤**
2. **납치**
3. **노인의 낙상 사고**

셋은 겉보기에 다르지만 하나의 공통점이 있다. 그리고 그 공통점이 이 제품의
설계 전체를 결정한다.

### 기존 SOS 앱이 깔고 있는 전제

거의 모든 안전 앱은 이렇게 작동한다.

> 위험을 감지한 사람이 → 폰을 꺼내 → 앱을 열고 → 버튼을 누른다

이 전제는 세 시나리오에서 **전부 깨진다.**

| 시나리오 | 깨지는 지점 |
|---|---|
| 술 취한 밤 | 새벽 두 시에 "지금부터 위험하다"고 판단할 정신이 없다. 애초에 위험한 줄도 모른다 |
| 납치 | 폰을 꺼낼 수 없다. 꺼내는 행동 자체가 위험을 키운다. 폰을 빼앗기면 끝이다 |
| 노인 낙상 | 쓰러진 뒤에는 폰까지 갈 수 없다. 폰이 손 닿는 데 있어도 조작이 안 된다 |

**"버튼을 누를 수 있는 사람"은 이미 최악의 상황이 아니다.**
진짜 위험한 순간에는 아무도 버튼을 누르지 못한다.

### 그래서 제품의 목표가 바뀐다

> 위급할 때 도움을 부르는 앱 → **위급해지기 전에 켜두면, 이후엔 알아서 남는 앱**

버튼은 여전히 있다. 누를 수 있는 상황이라면 그게 제일 빠르니까.
하지만 버튼은 **여러 진입로 중 하나**일 뿐이고, 나머지 진입로가 이 제품의
본체다.

## 1.2 세 시나리오를 각각 어떻게 푸는가

### 술 취한 밤 — "밤 모드"

판단이 멀쩡할 때(마시기 전) 한 번 켜두고 잊는다.

- 시작할 때 **아무에게도 문자를 보내지 않는다.** 공유가 아니라 기록이 목적이다
- 주기가 길다(10분). 밤새 돌아야 하니 배터리가 관건이다
- **끝나는 시각을 미리 정한다**(새벽 4시 / 6시 / 8시). 취한 사람은 종료도 안 누른다
- 다음 날 아침, 기록 화면에서 지도로 되짚는다

여기서 **사진 타임라인**이 결정적이다. 좌표는 기억을 되살리지 못한다.
"37.5632, 126.9851 에 40분 머무름"은 아무 말도 안 해주지만,
**"1시 40분, 여기서 사진 3장"** 은 그 자리를 떠올리게 한다.

그래서 그날 찍은 사진의 **촬영 시각과 GPS 를 동선에 겹쳐** 보여준다.
사진은 복사하지도 업로드하지도 않는다 — 기기 안에서 읽기만 한다.

### 납치 — "사전 연결"과 "보지 않고 발동"

납치 상황에서 문자 발송은 두 가지로 실패한다.

1. 문자 앱을 여는 동작이 눈에 띈다
2. 폰을 빼앗기거나 꺼지면 그 뒤로는 아무것도 안 간다

그래서:

- **사전 연결(마이페이지 → 연결).** 미리 맺어둔 사람은 문자 없이도
  앱에서 내 세션을 바로 본다. 그 순간에 아무 조작도 필요 없다
- **서버 워치독.** 폰이 꺼져서 신호가 끊기는 것 자체를 신호로 삼는다.
  가해자가 폰을 끄는 행동이 오히려 알림을 만든다
- (예정) **볼륨키 트리거** — 화면을 보지 않고 주머니 속에서 발동

### 노인 낙상 — "이상 감지"와 "지켜보는 쪽"

- **이상 감지**: 위치가 안 변하고 폰도 전혀 안 움직이는 상태가 이어지면
  확인 질문을 띄우고, 응답이 없으면 문자를 보낸다
- 이 시나리오에서 앱이 깔릴 폰은 **지켜보는 자식이 아니라 지켜봄을 받는
  부모의 폰**이다. 이 사실이 설치 난이도와 플랫폼 선택을 바꾼다
- 부모 폰이 안드로이드라면 개발 빌드가 무료이고, 문자 자동 발송까지 가능하다

## 1.3 제품 원칙

설계 판단이 갈릴 때 여기로 돌아온다.

**1. 켜는 순간의 부담을 0 에 가깝게.**
가입도 비밀번호도 없다. 앱을 처음 켤 때 서버에서 신원 한 쌍을 받아 기기에
저장하는 것이 곧 계정이다. 안전 앱에서 가입 절차는 그 자체로 이탈 요인이고,
우리가 이메일로 할 일도 없다.

**2. 판단은 미루고 기록은 남긴다.**
"이게 위험한가?"를 앱이 단정하지 않는다. 이상 감지는 **"바로 신고"가 아니라
"확인 질문"의 방아쇠**로만 쓴다. 카페에서 폰을 테이블에 올려둔 사람과
쓰러진 사람은 센서로 구분되지 않는다. 구분하려 들면 오탐으로 신뢰를 잃는다.

**3. 끊겨도 잃지 않는다.**
지하 주차장, 비행기 모드, 앱 강제 종료, 배터리 방전 — 전부 일어난다.
그때마다 기록이 통째로 사라지면 이 앱은 안전 앱이 아니다.

**4. 침묵을 신호로 읽는다.**
"아무 소식이 없다"는 안전하다는 뜻이 아니다. 이 앱에서는 **위험의 신호**다.

**5. 데이터가 앱에 갇히지 않는다.**
GPX 로 내보낼 수 있다. 등산·러닝 앱과 구글 어스가 읽는 표준이라,
이 앱이 사라져도 기록은 남는다.

**6. 무섭게 굴지 않는다.**
안전 앱은 겁을 팔기 쉽다. 이모지로 장식된 경고, 필요 없는 권한 요구,
과장된 알림. 그런 앱은 정작 필요할 때 이미 지워져 있다.
UI 는 조용하고, 권한은 실제로 쓰는 것만 요구한다.

## 1.4 사용자와 역할

한 사람이 두 역할을 오간다. 별도 계정이나 모드가 없다.

| 역할 | 하는 일 | 쓰는 화면 |
|---|---|---|
| **기록하는 사람** | 추적/밤 모드를 켜고 다닌다 | 홈, 추적, 밤 시작 |
| **지켜보는 사람** | 링크나 연결로 상대를 본다 | 지켜보기, 마이페이지, 웹 뷰어 |
| **되짚는 사람** | 지난 기록을 다시 본다 | 기록, 기록 상세 |

**연결은 항상 상호(mutual)다.** 한쪽만 보는 관계를 만들지 않았다.
일방적 위치 공유는 감시에 가까워지고, 가족·연인 관계에서 비대칭은 오히려
불신을 만든다. 수락 화면에서 이 점을 명시한다.

---

# 2부. 설계

## 2.1 전체 구조

```
 ┌──────────────────────────┐
 │  앱 (React Native/Expo)  │   기록하는 쪽 + 지켜보는 쪽
 │                          │
 │  화면 12  ·  lib 14      │
 └───────────┬──────────────┘
             │ RPC 만 호출 (테이블 직접 접근 없음)
             ▼
 ┌──────────────────────────┐
 │  Supabase (PostgreSQL)   │
 │                          │
 │  테이블 7 (전부 RLS 잠금) │
 │  함수 20 (SECURITY DEF.) │
 │  pg_cron → 워치독 (선택)  │
 └───────────┬──────────────┘
             │ 같은 RPC
             ▼
 ┌──────────────────────────┐
 │  웹 뷰어 (Vercel)         │   앱 없는 사람이 링크로 본다
 │  Leaflet + CARTO         │
 │  /t/:code                │
 └──────────────────────────┘
```

**핵심은 앱과 웹이 같은 RPC 를 쓴다는 것.** 권한 판정이 한 곳에만 있고,
클라이언트가 늘어도 보안 표면이 늘지 않는다.

## 2.2 데이터 모델

### v2 — 세션과 위치

```sql
sessions
  short_code       TEXT PK      -- 공유 링크의 열쇠
  owner_token      UUID         -- 비밀. 쓰기 권한
  user_name        TEXT
  started_at       TIMESTAMPTZ
  ended_at         TIMESTAMPTZ
  active           BOOLEAN
  interval_minutes SMALLINT     -- 보는 쪽이 "지금 늦은 건가" 판단하는 기준
  owner_user_id    UUID → profiles   (v3 에서 추가)
  alert_state      TEXT              (v4 에서 추가)
  alerted_at       TIMESTAMPTZ       (v4 에서 추가)

locations
  id           BIGSERIAL PK
  session_code TEXT → sessions
  latitude / longitude / accuracy  DOUBLE PRECISION
  updated_at   TIMESTAMPTZ
  battery      SMALLINT     -- 신호가 끊기기 전에 예고해준다
  shake        SMALLINT     -- 위치가 안 변해도 움직이는 중인지 알 수 있다
```

`battery` 와 `shake` 를 넣은 이유가 이 설계의 성격을 잘 보여준다.
둘 다 **"신호가 끊겼을 때 그게 무슨 뜻인지"를 해석하기 위한 재료**다.
배터리 4% 에서 끊긴 것과 80% 에서 끊긴 것은 완전히 다른 사건이다.

### v3 — 신원과 연결

```sql
profiles
  user_id      UUID PK      -- 공개 식별자
  owner_token  UUID         -- 비밀. 이 기기만 가진다
  display_name / emoji
  created_at / last_seen_at

invites
  code       TEXT PK
  from_user  UUID → profiles
  expires_at TIMESTAMPTZ
  used_by / used_at

links
  user_a UUID → profiles
  user_b UUID → profiles
  relation TEXT
  PRIMARY KEY (user_a, user_b)
  CHECK (user_a < user_b)      -- ★
```

`CHECK (user_a < user_b)` 는 작지만 중요하다. 같은 쌍이 (A,B) 와 (B,A)
두 줄로 생기는 것을 **데이터베이스 차원에서** 막는다. 애플리케이션 코드가
정규화를 잊어도 깨지지 않는다.

### v4 — 워치독

```sql
alerts
  session_code TEXT → sessions
  kind         TEXT       -- 'overdue' 등
  detail       JSONB
  created_at   TIMESTAMPTZ
  delivered    BOOLEAN    -- 같은 알림을 반복 발송하지 않기 위한 근거

watchdog_config
  user_id     UUID PK → profiles
  webhook_url TEXT        -- 비밀에 가깝다. 본인만 RPC 로 읽고 쓴다
  enabled     BOOLEAN
```

## 2.3 보안 설계

이 부분은 **한 번 크게 틀렸다가 다시 만든 곳**이라 자세히 남긴다.

### 처음의 잘못된 설계

RLS 를 켜고 `USING (true)` 정책을 달았다. 켜져 있으니 안전해 보였다.
실제로는 아무것도 막지 않았다.

Supabase 의 `anon` 키는 **원래 공개되는 값**이다. 앱 번들에도 웹 페이지
소스에도 들어간다. 즉 누구나 그 키를 얻을 수 있고, `USING (true)` 라면
누구나 이렇게 할 수 있었다.

```
GET /rest/v1/locations?select=*     →  전체 사용자의 모든 위치 기록
```

**공개된 키 + 개방 정책 = 공개 데이터베이스.**

### 다시 만든 설계

```
1. 모든 테이블에 RLS 를 켜고 정책을 하나도 만들지 않는다
   → 정책이 없으면 anon 은 아무것도 못 한다

2. REVOKE ALL ON <table> FROM anon, authenticated
   → 테이블 권한 자체를 회수한다

3. 필요한 동작만 SECURITY DEFINER 함수로 뚫는다
   → 함수 안에서 소유권을 검사한 뒤에만 데이터를 만진다
```

권한은 두 개의 비밀로 갈린다.

| 비밀 | 아는 사람 | 할 수 있는 것 |
|---|---|---|
| `short_code` | 링크를 받은 사람 | **읽기만** — 그 세션 하나의 위치 |
| `owner_token` | 기록하는 기기만 | **쓰기** — 위치 추가, 세션 종료 |

모든 쓰기 함수는 첫 줄에서 `here_assert_owner(user_id, owner_token)` 를
호출한다. 이 한 줄이 없으면 다른 사람의 세션에 위치를 심을 수 있다.

### short_code 를 추측할 수 있는가

```
알파벳: abcdefghjkmnpqrstuvwxyz23456789   (31자)
        └ 헷갈리는 0/o, 1/l, i 를 뺐다 — 전화로 불러줄 수 있어야 한다
길이:   8자리
공간:   31^8 ≈ 8.5 × 10^11
```

세션은 24시간 뒤 정리되므로, 그 안에 무작위로 맞출 확률은 실질적으로 0 이다.

### 검증

새 PostgreSQL 16 인스턴스에 `anon` / `authenticated` 역할을 만들어
**공격 시나리오 15가지**를 실제로 실행해 전부 차단되는 것을 확인했다.
테이블 직접 조회, 토큰 없는 쓰기, 남의 토큰으로 쓰기, 함수 오버로드 혼선 등.

### 남은 한계 (정직하게)

- **short_code 를 받은 사람은 그 세션을 계속 볼 수 있다.** 링크 회수 기능이
  없다. 세션이 24시간 뒤 지워지는 것이 사실상의 만료다
- **기기를 잃으면 신원도 잃는다.** `owner_token` 이 기기에만 있으므로
  기기 교체 시 연결이 끊긴다. 계정 이전은 별도 복구 수단이 필요한데,
  지금은 그 복잡도를 지지 않고 "연결을 다시 맺는다"로 처리한다
- **웹훅 주소는 서버에 평문으로 있다.** 테이블 접근을 막아 RPC 로만 읽게
  했지만, 데이터베이스를 통째로 가져가는 공격에는 노출된다

## 2.4 핵심 알고리즘

숫자를 고른 근거를 남긴다. 근거 없는 상수는 나중에 아무도 못 고친다.

### 지연 판정 — 언제 "신호가 끊겼다"고 볼 것인가

```sql
here_overdue_after(interval) = (interval × 2 + 2) 분
```

한 주기만 넘겨서 경고하면 GPS 취득이 조금 늦는 **흔한 상황마다** 울린다.
경보가 자주 울리는 안전 장치는 꺼진다. ×2 로 여유를 주고, +2 는 짧은 주기
(1분)에서 배수만으로는 여유가 너무 적은 것을 보정한다.

| 주기 | 지연 판정 |
|---|---|
| 1분 | 4분 |
| 5분 | 12분 |
| 10분 | 22분 |
| 15분 | 32분 |

### 이상 감지 — 왜 두 조건을 동시에 보는가

```js
DEFAULT_ANOMALY = {
  enabled: false,      // ★ 기본 꺼짐
  windowMinutes: 10,
  maxMoveMeters: 30,
  maxShake: 4,
}
```

**위치만 보면** 지하철·실내·GPS 음영에서 계속 오탐한다.
**흔들림만 보면** 책상에 올려둔 폰과 쓰러진 폰을 구분 못 한다.
둘이 **동시에** 죽어 있을 때만 의심하면 오탐이 크게 준다.

그래도 확신할 수는 없다. 카페에 앉아 폰을 테이블에 두면 똑같이 보인다.
그래서 이 판정은 **확인 질문의 방아쇠로만** 쓰고, 기본값은 꺼짐이다.
켤지 말지는 사용자가 자기 생활 패턴을 알고 정한다.

> **구현 중 잡은 버그**: 처음에는 판정 창(window) 안의 점만 봤더니,
> 전송 주기와 창 길이의 위상에 따라 같은 상황이 다르게 판정됐다.
> 창 **바깥의 기준점 하나**를 같이 보도록 고쳐 해결했다. 단위 테스트가
> 아니었으면 못 잡았을 종류의 버그다.

### 흔들림 지수

가속도 크기 `|a|` 는 가만히 있으면 중력 때문에 1g 근처에 고정된다.
움직이면 출렁이므로, **최근 5초 구간의 표준편차**가 곧 흔들림이다.

```
샘플링 200ms × 25개 = 약 5초 창
지수 = 표준편차 × 100

  0~4    거의 정지
  5~24   걷는 중
  25+    크게 흔들림
```

### 체류 구간 — 선만 보면 안 보이는 것

술집에 40분 앉아 있어도 지도에는 점이 한자리에 겹칠 뿐이라 지나간 길과
구분이 안 된다. **반경 60m 안에 5분 이상** 머문 구간을 따로 표시하면
동선이 이야기로 읽힌다.

중심점을 고정하지 않고 **점이 들어올 때마다 무게중심을 다시 계산**한다.
고정하면 천천히 이동하는 경우(가게 안에서 자리 옮기기)를 놓친다.

### GPS 이상치 제거

`web/lib/outlier.js` — mahlernim/google-timeline-visualizer (MIT) 에서
가져와 이 앱의 조건에 맞게 다시 맞췄다.

원본은 **수 개월치 구글 타임라인(항공편 포함)** 이 대상이라 500km 도약,
1300km/h 같은 상수를 쓴다. 여기서는 **1~15분 간격의 실시간 추적**이
대상이므로 지상 이동 규모로 다시 잡고, GPS accuracy 게이트를 추가했다.

```js
DEFAULT_ACCURACY_LIMIT_M   = 100   // 이보다 부정확하면 버린다
MIN_OUTLIER_HOP_KM         = 1     // 이 이상 튀어야 의심
MAX_REJOIN_DISTANCE_KM     = 5     // 튀었다가 이 안으로 돌아오면 이상치
MAX_PLAUSIBLE_SPEED_KMH    = 400
MAX_OUTLIER_RUN_POINTS     = 2     // 연속 2점까지만 이상치로 인정
```

핵심 판정은 **"멀리 튀었다가 원래 자리로 돌아왔는가"**(excursion)다.
진짜 이동은 돌아오지 않는다.

### 경로 재생 — 진행률을 무엇에 매핑할 것인가

**점 번호가 아니라 누적 거리에 매핑한다.**

점 번호로 재생하면 술집에 앉아 있던 구간(점이 몰려 있음)에서 재생이 기어가고,
택시 구간(점이 듬성함)에서 순간이동한다. **데이터 밀도가 곧 재생 속도가
돼버린다.** 누적 거리를 쓰면 지도 위에서 일정한 속도로 움직인다.

## 2.5 끊겨도 잃지 않기

세 겹으로 막는다.

### 1겹. 오프라인 큐 — 네트워크가 끊길 때

위치 전송이 실패하면 버리지 않고 큐에 쌓는다. 다음 전송이 성공하면
밀린 것을 함께 올린다. 이때 **`recorded_at` 을 같이 보내는 것**이 중요하다.
서버 시각으로 기록하면 지하 주차장에서 나온 순간에 30분치가 한 지점으로
뭉쳐버린다.

서버는 받은 시각을 그대로 믿지 않는다. 미래 시각과 지나치게 오래된 시각을
잘라내고, 배터리 값도 범위를 벗어나면 정리한다.

### 2겹. 활성 세션 저장 — 앱이 죽을 때

추적 상태를 화면 안 메모리에만 두면, 앱이 강제 종료되거나 OS 가 메모리를
회수하는 순간 두 가지가 **동시에** 사라진다.

1. `owner_token` — 이게 없으면 추적을 끝낼 수 없다. 공유 링크가 서버 정리
   (24시간) 전까지 계속 살아있게 된다
2. 그때까지 쌓인 위치 — 기록이 통째로 증발한다

그래서 **위치를 하나 찍을 때마다 상태 전체를 다시 저장한다.** 세션 하나가
수백 점 규모라 직렬화 비용은 무시할 만하고, 대신 어느 시점에 죽어도
**최대 한 점만** 잃는다.

### 3겹. 서버 워치독 — 폰이 죽을 때

앞의 두 겹은 앱이 살아 있어야 동작한다. **폰이 꺼지면 아무것도 못 한다.**

그래서 판단을 서버로 옮겼다. `pg_cron` 이 주기적으로 `here_run_watchdog()`
을 돌려, 약속한 주기보다 오래 소식이 없는 세션을 찾아 `alerts` 에 남기고,
웹훅이 설정돼 있으면 `pg_net` 으로 보낸다.

**이것이 "침묵을 신호로 읽는다"는 원칙의 구현이다.**
납치범이 폰을 끄는 행동이 오히려 알림을 만든다.

`pg_cron` / `pg_net` 은 **선택**이다. 없어도 신호 끊김 "표시"는 앱과 웹
양쪽에서 그대로 동작하고, 자동 발송만 못 쓴다.

## 2.6 프라이버시

- **사진은 기기 밖으로 나가지 않는다.** 촬영 시각, (있으면) GPS 좌표,
  미리보기 URI 만 읽는다. 복사도 업로드도 없다
- **기록은 내 폰에 남는다.** 서버의 위치 데이터는 24시간 뒤 정리되지만
  기기 이력은 최근 30개 세션을 보관한다
- **연결은 상호이고, 끊을 수 있다**(`here_unlink`)
- **초대 코드는 만료된다.** 만료된 것은 `here_purge_invites` 로 정리한다
- **`anon` 키는 공개돼도 안전하다.** 앱 번들, 웹 소스, `eas.json` 어디에
  있어도 문제없다. 실제 방어는 RLS 와 RPC 가 한다.
  반면 **`service_role` 키는 절대 클라이언트에 두면 안 된다**

## 2.7 화면 구조

단일 상태 변수(`screen`)로 전환한다. 네비게이션 라이브러리를 쓰지 않았다 —
화면 수가 적고 흐름이 단순해서, 라이브러리가 주는 이득보다 의존성과 번들
크기가 크다.

```
최초 실행 ──▶ 온보딩 ──▶ 설정(최초) ──┐
                                      ▼
                                    ┌────┐
        ┌───────────────────────────│ 홈 │───────────────────────┐
        │                           └────┘                       │
        ▼            ▼           ▼      ▼        ▼        ▼      ▼
     추적 시작   밤 모드 시작   기록   지켜보기  마이페이지  체크인  설정
        │            │           │                 │
        ▼            ▼           ▼                 ├─▶ 연결
     [추적 중] ◀─────┘      기록 상세              └─▶ 워치독
        │                    (지도·재생·
        └─▶ 문자·링크 공유    체류·사진)
```

| 화면 | 역할 |
|---|---|
| 온보딩 / 설정 | 이름, 연락처, 문자 문구, 전송 주기 |
| 홈 | SOS 버튼(2초 홀드), 각 기능 진입 |
| 밤 시작 | 종료 시각 선택 후 조용히 시작 |
| 추적 중 | 현재 상태, 거리·속도·흔들림, 링크 공유, 종료 |
| 기록 / 기록 상세 | 지난 세션, 지도·재생·체류·사진 타임라인, GPX |
| 지켜보기 | 코드나 링크로 남을 본다 |
| 마이페이지 | 내 신원, 연결된 사람들, 워치독 설정 |
| 연결 | 초대 코드 만들기 / 받기 |
| 체크인 | 데드맨 스위치 (15/30/60/120분) |

SOS 버튼은 **2초 홀드 + 원형 진행 표시**다. 주머니 속 오작동을 막으면서,
누르고 있는 동안 "지금 뭐가 되고 있는지" 보여준다.

## 2.8 설정값 한눈에

| 항목 | 값 | 근거 |
|---|---|---|
| 추적 주기 | 1 / 5 / 10 / 15분 | 배터리와 해상도의 교환 |
| 밤 모드 주기 | 10분 고정 | 밤새 돌아야 한다 |
| 밤 모드 종료 | 새벽 4 / 6 / 8시 | 취한 사람은 종료를 안 누른다 |
| 체크인 | 15 / 30 / 60 / 120분 | |
| 이상 감지 | 10분 / 30m / 흔들림 4 | 기본 꺼짐 |
| 체류 판정 | 반경 60m / 5분 | |
| GPS 정확도 한계 | 100m | 이보다 부정확하면 버린다 |
| 기기 이력 보관 | 30 세션 | 세션당 ~15KB |
| 서버 데이터 정리 | 24시간 | |
| 코드 공간 | 31^8 ≈ 8.5 × 10^11 | |

## 2.9 기술 선택과 이유

| 선택 | 이유 | 대가 |
|---|---|---|
| **Expo (RN)** | 한 코드로 iOS·Android·웹. 혼자 만드는 앱에서 결정적 | 네이티브 기능은 개발 빌드가 필요 |
| **Supabase** | PostgreSQL 을 그대로 쓴다. RLS 와 SECURITY DEFINER 로 권한을 DB 안에 둘 수 있다 | 벤더 종속 |
| **RPC 전용 접근** | 권한 판정이 한 곳에만 있다 | 함수 시그니처 관리 필요 |
| **기기 = 계정** | 가입 절차가 0 | 기기를 잃으면 신원도 잃는다 |
| **Leaflet + CARTO** | API 키 없이 뜬다. 링크를 받은 사람이 아무것도 안 해도 된다 | 구글 지도만큼 상세하진 않다 |
| **네비게이션 라이브러리 없음** | 의존성과 번들 절약 | 화면이 늘면 재검토 |
| **정적 웹 뷰어** | Vercel 무료. 앱 없이도 본다 | |

---

# 3부. 현실

## 3.1 플랫폼별로 되는 것과 안 되는 것

| | Expo Go<br>(무료, 지금) | 개발 빌드<br>iOS ($99/년) | 개발 빌드<br>Android (무료) |
|---|:---:|:---:|:---:|
| 위치 기록·공유 | ✓ | ✓ | ✓ |
| 사진 타임라인 | ✓ | ✓ | ✓ |
| 사람 연결 | ✓ | ✓ | ✓ |
| 서버 워치독 | ✓ | ✓ | ✓ |
| 흔들림·이상 감지 | ✓ (앱이 떠 있을 때) | ✓ | ✓ |
| **백그라운드 추적** | **✗** | ✓ | ✓ |
| 잠금화면 | ✗ | Live Activity | 위젯 |
| 낙상 감지 | ✗ | 제한적¹ | ✓ |
| 볼륨키 트리거 | ✗ | **✗**² | ✓ |
| 문자 자동 발송 | ✗ | **✗**³ | ✓ |

¹ iOS 는 백그라운드 고빈도 가속도계를 제한한다
² iOS 는 백그라운드에서 볼륨키를 가로챌 수 없다
³ iOS 정책상 영구 불가

### 백그라운드가 유일하고 진짜인 이유

Expo Go 에서는 **앱이 화면에 떠 있어야** 위치가 올라간다. 화면을 끄거나
다른 앱으로 넘어가면 멈춘다.

이건 세 시나리오 전부를 무력화한다. 취한 사람이 밤새 앱 화면을 켜두지 않고,
납치범이 앱을 열어둔 채로 두지 않으며, 쓰러진 노인의 폰은 주머니 안에 있다.

**따라서 개발 빌드는 "있으면 좋은 것"이 아니라 제품이 성립하기 위한 조건이다.**

### 문자 자동 발송을 못 하는 게 덜 아픈 이유

iOS 는 앱이 사용자 몰래 문자를 보내는 것을 영원히 허용하지 않는다.
안전 앱에게는 치명적으로 들린다.

그런데 우리는 이미 **사전 연결**을 만들어놨다. 연결된 사람은 문자 없이도
앱에서 바로 내 상태를 본다. **문자는 연결 안 된 사람을 위한 보조 수단**이지
주 경로가 아니다.

이건 우연이 아니라, "그 순간에 아무 조작도 못 한다"는 문제 정의에서
자연스럽게 나온 결론이었다. 설계가 맞았으면 플랫폼 제약이 덜 아프다.

## 3.2 알려진 한계

**제품**
- 이상 감지는 확신할 수 없다. 그래서 기본이 꺼짐이고 확인 질문만 띄운다
- 링크를 한 번 받은 사람은 세션이 끝날 때까지 볼 수 있다. 회수 기능이 없다
- 기기를 바꾸면 신원과 연결이 끊긴다
- 실내·지하에서 GPS 는 여전히 부정확하다. 이상치 제거는 완화일 뿐 해결이 아니다

**운영**
- `pg_cron` 없이는 워치독 자동 발송이 안 된다 (표시는 됨)
- 웹훅 주소가 서버에 평문으로 있다
- 무료 티어의 EAS 빌드 한도와 대기열이 개발 속도를 제약한다

**아직 안 한 것**
- 자동화된 회귀 테스트가 순수 함수 몇 개에만 있다. 화면은 수동 확인
- 다국어 없음 (한국어 전용)
- 접근성 검토를 안 했다. 노인 사용자를 겨냥한 앱에서 이건 실제 결함이다

## 3.3 로드맵

### 지금 (Expo Go, 무료)

아이폰에서 바로 쓸 수 있다. 밤에 한 번 켜보고 실제로 쓸 만한지 확인하는
단계. **여기서 얻는 판단이 다음 지출을 정당화한다.**

### 다음 — 개발 빌드

| 순서 | 기능 | 필요한 것 |
|---|---|---|
| 1 | **백그라운드 추적** | `expo-task-manager`, 백그라운드 위치 권한, Android 포그라운드 서비스 알림 |
| 2 | **낙상 감지** | 백그라운드 가속도계. 1번이 먼저 |
| 3 | **잠금화면** | iOS Live Activity / Android 위젯 |
| 4 | **볼륨키 트리거** | 네이티브 모듈. Android 만 |
| 5 | **문자 자동 발송** | `SEND_SMS` + 네이티브 모듈. Android 만 |

1번이 나머지 전부의 전제다. **밤 모드가 여기서 제 값을 한다** —
"앱을 켜둬야 한다"는 제약이 사라지는 순간, 마시기 전에 켜두고 잊는다는
원래 설계가 비로소 성립한다.

### 나중에 (검토 대상, 결정된 것 아님)

- 계정 복구 / 기기 이전
- 링크 회수
- 접근성 (큰 글씨, 음성 안내) — 노인 시나리오를 진지하게 한다면 필수
- 스토어 출시 (Google Play $25 일회 / App Store 는 위 $99 계정)

## 3.4 의사결정 기록

나중에 "왜 이렇게 했더라"가 될 것들.

| 결정 | 대안 | 왜 이쪽인가 |
|---|---|---|
| RPC 전용 접근 | RLS 정책으로 세밀 제어 | 공개 anon 키 환경에서 정책 하나만 느슨해도 전부 뚫린다. 실제로 그렇게 뚫려 있었다 |
| 기기 = 계정 | 이메일 가입 | 안전 앱에서 가입 절차는 이탈 요인. 우리가 이메일로 할 일도 없다 |
| 상호 연결만 | 일방 공유 허용 | 일방적 위치 공유는 감시에 가깝다. 비대칭은 관계에 불신을 만든다 |
| 이상 감지 기본 꺼짐 | 기본 켜짐 | 오탐하는 안전 장치는 꺼진다. 켜는 판단은 사용자가 |
| 이모지 UI 제거 | 이모지로 친근하게 | 안전 앱이 장식적이면 신뢰를 잃는다. SVG 아이콘 세트로 교체 |
| 로고를 SOS 마크에서 `h` 마크로 | SOS 마크 유지 | 화면에 SOS 를 크게 박아두면 남에게 보이기 부담스럽다. 밤 모드처럼 평상시에 켜두는 기능과 안 맞는다 |
| `SEND_SMS` 권한 제거 | 미리 선언해두기 | 지금은 시스템 문자 앱을 여는 방식이라 필요 없다. 안 쓰는 위험 권한을 요구하면 설치에서 이탈한다 |
| 누적 거리 기반 재생 | 점 번호 기반 | 점 번호를 쓰면 데이터 밀도가 재생 속도가 된다 |
| 앱/웹 geo 모듈 분리 사본 | 공유 모듈 | 네이티브 번들에 `web/` 을 끌어오지 않기 위해. 같은 공식을 쓴다 |

## 3.5 이 프로젝트에서 배운 것

세 번은 **테스트가 아니었으면 그대로 나갔을 결함**이었다.

1. **개방 RLS 정책** — 스스로 만든 보안 구멍. "RLS 켜짐"이 "안전"이 아니다
2. **이상 감지 위상 버그** — 전송 주기와 판정 창의 위상에 따라 같은 상황이
   다르게 판정됐다. 단위 테스트가 잡았다
3. **스키마 재실행 실패** — `DROP FUNCTION` 시그니처를 손으로 나열해두고
   인자를 추가했다. 두 번째 실행이 막혔다. **손으로 유지하는 목록은 반드시
   드리프트한다** — 이름으로 찾아 지우도록 바꿨다
4. **거짓 OK** — 위를 고친 뒤, 점검 스크립트가 함수를 이름으로만 세고 있어서
   구버전으로 되돌아간 상태에도 20/20 OK 를 찍었다. 점검이 통과한다는 것과
   올바르다는 것은 다르다

공통점: **"될 것 같다"와 "확인했다" 사이의 거리.** 스키마는 실제
PostgreSQL 에 세 번 연속 실행해보고, 화면은 브라우저로 열어보고, 순수 함수는
단위 테스트로 확인하는 습관이 매번 무언가를 잡아냈다.

---

## 부록. 용어

| 용어 | 뜻 |
|---|---|
| **세션** | 추적 한 번. 시작~종료까지의 위치 묶음 |
| **short_code** | 세션의 공개 열쇠. 8자리. 링크에 들어간다 |
| **owner_token** | 세션·계정의 비밀 열쇠. 쓰기에 필요. 기기에만 있다 |
| **연결(link)** | 두 사람의 상호 관계. 문자 없이 서로를 볼 수 있다 |
| **워치독** | 신호가 끊긴 세션을 서버가 찾아내는 장치 |
| **지연(overdue)** | 약속한 주기보다 오래 새 위치가 없는 상태 |
| **체류(stay)** | 반경 안에 일정 시간 이상 머문 구간 |
| **흔들림 지수** | 최근 5초 가속도 표준편차 × 100 |
| **밤 모드** | 마시기 전에 켜두는, 종료 시각이 예약된 기록 모드 |
| **데드맨 스위치** | 정해진 시간 안에 응답이 없으면 발동하는 장치 |

## 부록. 서드파티 출처

- GPS 이상치 제거 · 애니메이션 이징:
  [mahlernim/google-timeline-visualizer](https://github.com/mahlernim/google-timeline-visualizer)
  (MIT, © 2025 mahlernim) — 실시간 추적 조건에 맞게 임계값을 다시 맞췄다.
  전문은 `web/lib/ATTRIBUTION.md`
- 지도 타일: CARTO Voyager / OpenStreetMap
- 지도 라이브러리: Leaflet
